import { FC, ReactNode } from 'react';
type NavigationItemContentProps = {
    children: ReactNode;
    isActive?: boolean;
};
/**
 * Content for the NavigationItem. Must be wrapped in a NavigationItem or it will throw an Error.
 * @param children
 * @param isActive
 * @constructor
 */
export declare const NavigationItemContent: FC<NavigationItemContentProps>;
export {};
