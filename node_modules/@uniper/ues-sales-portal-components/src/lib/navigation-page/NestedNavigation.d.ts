import { SxProps, Theme } from '@mui/material';
import { FC, ReactNode } from 'react';
type NestedNavigationProps = {
    children: ReactNode;
    sx?: SxProps<Theme>;
};
export declare const NestedNavigation: FC<NestedNavigationProps>;
export {};
