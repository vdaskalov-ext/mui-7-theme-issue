import { FC } from 'react';
export declare enum AvatarSize {
    SMALL = 16,
    REGULAR = 24
}
export declare enum AvatarType {
    NUMERIC = "NUMERIC",
    ALPHABETIC = "ALPHABETIC"
}
type NavigationItemBulletProps = {
    bulletValue?: number;
    avatarSize?: AvatarSize;
    avatarType?: AvatarType;
    error?: boolean;
    ghost?: boolean;
    isActive?: boolean;
};
/**
 * Bullet point for the NavigationItem. Must be wrapped in a NavigationItem or it will throw an Error.
 * @throws Error if used outside of a NavigationItem.
 * @param bulletValue
 * @param error
 * @param ghost
 * @param avatarSize
 * @param avatarType
 * @param overrideIsActive
 * @constructor
 */
export declare const NavigationItemBullet: FC<NavigationItemBulletProps>;
export {};
