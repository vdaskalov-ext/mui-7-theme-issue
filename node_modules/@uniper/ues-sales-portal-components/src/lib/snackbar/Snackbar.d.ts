import { AlertColor, SnackbarOrigin } from '@mui/material';
import { FC, PropsWithChildren, ReactNode } from 'react';
type SnackbarOptions = {
    variant: AlertColor;
    key: string;
    action: ReactNode;
    autoHideDuration: number | null;
    anchorOrigin?: SnackbarOrigin;
};
type SnackbarContextType = {
    enqueueSnackbar: (content: ReactNode, options?: Partial<SnackbarOptions>) => string;
    closeSnackbar: (key: string) => void;
};
type ProviderOptions = {
    anchorOrigin?: SnackbarOrigin;
    autoHideDuration?: SnackbarOptions['autoHideDuration'];
};
export declare const SnackbarProvider: FC<PropsWithChildren<ProviderOptions>>;
export declare const useSnackbar: () => SnackbarContextType;
export {};
