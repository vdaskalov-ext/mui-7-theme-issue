import { SalesPortalRole } from './user';
/**
 * Check if the currently logged-in user has at least one of the allowed roles.
 * @param allowedRoles the roles to check
 * @returns true if the user has at least one of the allowed roles, otherwise false
 */
export declare const useUserAccessControl: (allowedRoles: SalesPortalRole[]) => boolean;
