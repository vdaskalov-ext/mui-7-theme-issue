import { FC, PropsWithChildren } from 'react';
import { DisableRule } from '../../model/DisableRule';
interface DisableFieldsProps {
    rules: DisableRule[];
    fullWidth?: boolean;
}
export declare const notDisabled: DisableRule;
/**
 * Disables all form controls that are wrapped within
 * WARNING - This component should NOT be used to disable single form controls (buttons, inputs, etc.)
 * Use DisabledFormControlTooltip instead and disable your desired form control directly
 * There is significant performance penalty using this component and it is meant for disabling multiple form controls at once
 * @param children - The form controls to be disabled
 * @param rules - An array of rules that determine if the fields should be disabled
 * @param fullWidth - If true, the fields will be displayed in full width
 */
export declare const DisableFields: FC<PropsWithChildren<DisableFieldsProps>>;
export {};
