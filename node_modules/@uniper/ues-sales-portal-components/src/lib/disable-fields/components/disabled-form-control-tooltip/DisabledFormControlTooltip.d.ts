import { TooltipProps } from '@mui/material';
import { FC, PropsWithChildren } from 'react';
import { DisableRule } from '../../model/DisableRule';
type Props = {
    rules: DisableRule[];
    fullWidth?: boolean;
};
export declare const DisabledFormControlTooltip: FC<PropsWithChildren<Props & Pick<TooltipProps, 'placement' | 'followCursor'>>>;
export {};
