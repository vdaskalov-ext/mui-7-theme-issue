import { FC, PropsWithChildren } from 'react';
type Props = {
    /**
     * The maximum width of the component in px.
     */
    maxWidth?: number;
};
export declare const WidthLimiter: FC<PropsWithChildren<Props>>;
export {};
